"use strict";

const set = 10;

const calcAge = (birthday) => {
    0;
    console.log("23");
    console.log();
};

calcAge();

const measureKelvin = function () {
    const measurement = {
        type: "temp",
        unit: "celisus",
        value: 10, //prompt("Degrees celcius: "),
    };

    console.log(measurement);

    const kelvin = Number(measurement.value) + 273;
    return kelvin;
};

console.log(measureKelvin());
